[Unit]
Description=KnowBase FastAPI backend
After=network.target docker.service

[Service]
User=root
WorkingDirectory=/opt/knowbase/backend
ExecStart=/opt/knowbase/backend/.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
